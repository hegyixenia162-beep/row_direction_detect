# `run_hsv_hough_v2.py` 逐行解析（零基础版）

> 对应脚本：`run_hsv_hough_v2.py`（271 行）。本文按当前工作区中的代码解释；空行仅用于排版，不重复说明。

## 1. 先用一句话说清它在做什么

这个脚本从无人机棉田照片中找出绿色植被，把规律排列的植被识别成棉花种植行，最后输出：二值植被掩膜图、画有橙色行线的结果图、每条行线坐标的 JSON，以及批处理汇总 JSON。

它不是深度学习模型，而是一条传统视觉流水线：

```text
读图 → 缩小工作图 → HSV 强植被分割 → ExG 弱苗补充
    → Hough 估计全局方向 → 判断是否存在上下两个方向区域
    → 各区域旋正 → 横向投影找峰（每个峰≈一条行）
    → 鲁棒直线拟合 → 映射回原图 → 画线并保存 JSON
```

一个必须先澄清的事实：V2 通过第 16 行导入 `run_hsv_hough.py`，并复用了其中 6 个函数。因此，V2 文件只有和 V1 不同或加强的部分，并非完整算法的全部实现。

## 2. 运行方式与默认产物

在项目目录执行：

```powershell
python run_hsv_hough_v2.py
```

默认读取 `test_photo/photo1.jpg` 和 `test_photo/photo2.jpg`，写入 `outputs_v2`。也可覆盖默认参数，例如：

```powershell
python run_hsv_hough_v2.py --names photo3.jpg photo4.jpg --max-side 1600
```

每张成功处理的图会产生：

- `图片名_mask_v2.png`：白色代表检测到的植被，黑色代表背景；
- `图片名_rows_v2.png`：原图上叠加橙色种植行；
- `图片名_rows_v2.json`：方向、行距、行端点和质量指标；
- `run_summary_v2.json`：本次所有图片的成功/失败摘要。

## 3. 逐行解析

### 第 1–16 行：说明、导入依赖

- **第 1 行** `"""..."""`：模块文档字符串，相当于文件的说明牌。它说明 V2 针对两张大图改了三件事：旋转方向、分区多方向、弱苗增强。
- **第 2 行**：空行，只为提高可读性。
- **第 3 行** `from __future__ import annotations`：让类型注解延迟解析。这样 `list[...]`、`tuple[...]` 等注解更稳定，也不会在函数定义时立刻求值；它不参与图像计算。
- **第 4 行**：空行。
- **第 5 行** `import argparse`：导入命令行参数工具，使用户可通过 `--input`、`--max-side` 等修改配置。
- **第 6 行** `import json`：用于把检测结果转换成 JSON 文本。
- **第 7 行** `import math`：提供角度/弧度转换、正弦、余弦和向下取整等数学函数。
- **第 8 行** `from dataclasses import asdict`：把 V1 中的 `RowSegment` 数据类对象转换成普通字典，方便保存为 JSON。
- **第 9 行** `from pathlib import Path`：用面向对象方式拼接目录和文件名，避免手写路径分隔符。
- **第 10 行**：空行。
- **第 11 行** `import cv2`：导入 OpenCV，负责颜色转换、形态学、连通域、旋转、直线拟合、读写图片等。
- **第 12 行** `import numpy as np`：导入 NumPy，负责数组、矩阵、统计和坐标运算。图像在程序中本质上就是 NumPy 数组。
- **第 13 行**：导入一维高斯滤波器，用于平滑投影曲线，减少叶片间断和像素噪声造成的小毛刺。[1]
- **第 14 行**：导入峰值检测器；平滑曲线上的一个峰通常对应一条种植行。[2]
- **第 15 行**：空行。
- **第 16 行** `import run_hsv_hough as v1`：把 V1 脚本作为模块导入，并取短名 `v1`。V2 后面会调用 V1 的强掩膜、缩放、Hough 方向、行距、采样、绘图函数。

### 第 19–33 行：读取命令行参数 `parse_args`

- **第 19 行**：定义函数 `parse_args`；箭头后的 `argparse.Namespace` 表示它返回一个装有参数的对象。
- **第 20 行**：函数说明。这里说“默认”只处理两张图；用户仍可用 `--names` 改成其他图片。
- **第 21 行**：空行。
- **第 22 行**：创建参数解析器；`description` 会显示在 `python ... --help` 的帮助信息中。
- **第 23 行**：定义 `--input`，默认输入目录是 `test_photo`。
- **第 24 行**：定义 `--output`，默认输出目录是 `outputs_v2`。
- **第 25 行**：定义 `--names`；`nargs="+"` 表示后面至少接一个文件名。默认列表只有 `photo1.jpg`、`photo2.jpg`。
- **第 26 行**：定义工作图最长边，默认 1800 像素。大图缩小后计算更快，但数值阈值也都作用在缩小后的“工作尺度”。
- **第 27 行**：强植被的 HSV 色相 H 下限为 25。
- **第 28 行**：强植被的 H 上限为 100。在 OpenCV 8 位 HSV 中，H 通常用 0–179 表示，而不是常见的 0–360。
- **第 29 行**：饱和度 S 下限为 35；排除大量接近灰色的像素。
- **第 30 行**：亮度 V 下限为 25；排除非常暗的像素。
- **第 31 行**：强掩膜连通域至少 18 个工作图像素；小于它的斑块会被当作噪声删除。
- **第 32 行**：Hough 主方向聚类允许相差 12°；这是“哪些候选线算同一方向”的容差。
- **第 33 行**：真正读取命令行并返回结果。之后可用 `args.h_min` 这样的写法取值。

### 第 36–69 行：制作“强植被 + 弱苗增强”掩膜

- **第 36 行**：定义 `build_enhanced_mask`。输入是 BGR 彩色图和参数；返回两个二维数组：强掩膜、增强掩膜。
- **第 37 行**：说明设计原则：弱苗可以降低 HSV 要求，但还必须呈现绿色优势，避免把土壤一起放进来。
- **第 38 行**：空行。
- **第 39 行**：调用 V1 的 `build_hsv_mask`。其内部流程是 BGR→HSV、阈值分割、3×3 开运算、5×5 闭运算，再删除面积小于 `min_component` 的连通域。
- **第 40 行**：把 OpenCV 读入的 BGR 图转换为 HSV。注意 OpenCV 默认通道顺序是 B、G、R，不是 RGB。
- **第 41 行**：把 BGR 三通道拆成 `blue`、`green`、`red` 三个二维数组。
- **第 42–44 行**：把三个通道从 `uint8` 转成有符号 16 位整数。原因是后面有减法；若仍用 0–255 的无符号整数，负结果可能发生回绕，造成错误。
- **第 45 行**：计算过量绿指数 `ExG = 2G - R - B`。绿色越明显，该值通常越大；它是脚本中的经验型颜色证据，不等于经过辐射定标的农学指数。
- **第 46 行**：空行。
- **第 47 行**：注释解释为何放宽弱苗条件：小叶片与土壤混合后，饱和度、亮度和绿色纯度都可能下降。
- **第 48 行**：开始构造一组布尔条件。每个像素最终只会得到 `True`（候选弱苗）或 `False`。
- **第 49 行**：H 至少为 `h_min-10`，但不能低于 15。`max` 是在两个下限中取更严格的那个。
- **第 50 行**：H 至多为 `h_max+10`，但不能高于 115。`min` 防止范围无限放宽。
- **第 51 行**：S 下限比强植被低 10，但最低仍为 20。
- **第 52 行**：V 下限比强植被低 10，但最低仍为 15。
- **第 53 行**：要求 ExG 至少 30，即总体上确有绿色优势。
- **第 54 行**：额外要求 `G-R ≥ 8`，防止某些蓝色很低但红绿接近的土壤像素仅靠 ExG 过关。
- **第 55 行**：结束多条件表达式。`&` 表示对每个像素逐项执行逻辑“并且”，所以六项必须同时满足。
- **第 56 行**：把 `True` 变成 255（白），`False` 变成 0（黑），并转换为 OpenCV 常用的 8 位掩膜。
- **第 57 行**：对弱掩膜执行 3×3 椭圆核开运算。开运算可理解为“先腐蚀再膨胀”，用于清掉细小孤立噪点。[3]
- **第 58 行**：做 8 邻域连通域标记。斜对角接触也算相连；返回连通域数量、每像素标签、各域统计量和质心。
- **第 59 行**：创建与 `weak` 同尺寸、全黑的空掩膜，准备只写回合格连通域。
- **第 60 行**：注释说明弱苗面积门槛有意小于强掩膜的 18 像素。
- **第 61 行**：从标签 1 遍历到最后一个连通域。标签 0 是背景，所以跳过。
- **第 62 行**：说明 6 像素是弱苗连通域的最低面积。
- **第 63 行**：条件判断：当前域面积是否至少 6 像素。
- **第 64 行**：若合格，把所有属于当前标签的像素写成白色。
- **第 65 行**：解释弱苗不能在全图任意补充，只能靠近可靠的强植被。
- **第 66 行**：用 9×9 椭圆核膨胀强掩膜，形成“强植被周边走廊”。膨胀会扩大白色区域。[3]
- **第 67 行**：对 `weak_clean` 和走廊按位与；只留下强植被附近的弱候选。
- **第 68 行**：按位或合并强植被和附近弱苗，得到最终增强掩膜。
- **第 69 行**：同时返回强掩膜和增强掩膜。强掩膜用于更稳的方向判断，增强掩膜用于尽量完整地找行。

### 第 72–85 行：构造正确的旋转矩阵

- **第 72 行**：定义旋转函数。`shape` 是 `(高, 宽)`，`angle_deg` 是种植行相对 x 轴的轴向角；返回 2×3 仿射矩阵和旋转后画布尺寸。
- **第 73 行**：指出 V1 的旋转符号问题是 V2 的修正点。
- **第 74 行**：空行。
- **第 75 行**：从形状中取出高和宽。
- **第 76 行**：计算图像中心 `(宽/2, 高/2)`，后续绕中心旋转。
- **第 77 行**：解释 OpenCV 正角是逆时针；在图像坐标系中必须谨慎处理方向符号。[4]
- **第 78 行**：计算实际旋转角 `原轴角-90°`，目标是让行轴转到竖直方向。
- **第 79 行**：生成围绕中心、缩放比例 1.0 的 2×3 旋转仿射矩阵。[4]
- **第 80 行**：从矩阵取出旋转对应的余弦和正弦绝对值，用于估算完整容纳旋转图的新画布。
- **第 81 行**：计算新画布宽度。旋转后原高和原宽在水平方向上的投影相加。
- **第 82 行**：计算新画布高度。
- **第 83 行**：修正矩阵的 x 平移，让旋转后的内容居中新画布，否则会被裁切或偏到一边。
- **第 84 行**：同理修正 y 平移。
- **第 85 行**：返回矩阵，以及 OpenCV 需要的 `(新宽, 新高)`。

### 第 88–107 行：衡量某个方向是否呈现“行周期”

- **第 88 行**：定义周期性得分函数。输入二值掩膜和候选方向，输出一个浮点分数。
- **第 89 行**：核心思想：沿“垂直于行”的方向投影。如果确有等间距作物行，投影密度会高低交替。
- **第 90 行**：空行。
- **第 91 行**：取得掩膜的高和宽。
- **第 92 行**：为每个像素生成 y、x 坐标矩阵。
- **第 93 行**：把角度转为弧度，因为 Python 三角函数使用弧度。
- **第 94 行**：构造行方向的法向量 `(-sinθ, cosθ)`，即与行方向垂直的单位向量。
- **第 95 行**：计算每个像素在法向轴上的投影坐标。直观上是在问：“这个像素位于第几条横跨行方向的统计带？”
- **第 96 行**：找到最小投影坐标并向下取整，为后面把坐标平移到非负整数做准备。
- **第 97 行**：减去最小值后向下取整，得到每个像素所属的一维箱编号，再拉平成一维。
- **第 98 行**：统计每个箱总共有多少图像像素，即分母。
- **第 99 行**：统计每个箱中有多少植被像素，即分子；`mask>0` 会把白色变成 `True/1`。
- **第 100 行**：分子除以分母，得到每个箱的植被密度；`maximum(...,1)` 防止除以零。
- **第 101 行**：用 `sigma=2` 的一维高斯滤波平滑密度曲线，减少像素级抖动。[1]
- **第 102 行**：只把横截长度超过图像最大边 5% 的箱视为有效，排除旋转投影两端很短的斜角区域。
- **第 103 行**：真正筛掉无效箱。
- **第 104 行**：注释说明过短投影会虚假放大方差。
- **第 105 行**：如果有效曲线不足 20 个点，或平均植被密度几乎为零，就无法可信评分。
- **第 106 行**：上述情况下返回 0。
- **第 107 行**：返回“标准差÷均值”，即变异系数。密度起伏相对平均值越强，分数越大，越像清晰的周期行。

### 第 110–138 行：判断图片是否有上下两个方向区域

- **第 110 行**：定义区域方向检测函数；返回若干 `(起始y, 结束y, 方向角)`。
- **第 111 行**：设计目标是识别一次上下方向切换，不是任意数量或任意形状地块。
- **第 112 行**：空行。
- **第 113 行**：读取图像高度。
- **第 114 行**：固定把图像水平切成 12 条。
- **第 115 行**：计算与全局方向正交的方向，并用 `%180` 把轴向角限制到 0–180°。
- **第 116 行**：创建布尔投票列表，记录每个条带是否更支持正交方向。
- **第 117 行**：说明这里不用局部 Hough，是为降低短叶缘线的误导。
- **第 118 行**：遍历 0 到 11 共 12 个条带。
- **第 119 行**：按比例计算当前条带顶部 y 坐标。
- **第 120 行**：计算底部 y 坐标。
- **第 121 行**：从强掩膜切出当前水平条带。
- **第 122 行**：计算该条带对全局方向的周期性得分。
- **第 123 行**：计算该条带对正交方向的得分。
- **第 124 行**：只有正交得分超过全局得分的 1.15 倍才投 `True`；15% 裕量可减少轻微波动导致的误切换。
- **第 125 行**：空行。
- **第 126 行**：初始化切换位置为 `None`，表示尚未找到。
- **第 127 行**：说明必须连续两个条带支持正交方向。
- **第 128 行**：遍历相邻条带对；最后一个索引只到 10，便于访问 `index+1`。
- **第 129 行**：说明只找第一次切换，这是针对当前两张大图的专用假设。
- **第 130 行**：当前条和下一条都为 `True` 时，认为方向切换稳定出现。
- **第 131 行**：保存第一次正交条带的索引。
- **第 132 行**：立即停止搜索，后续可能的方向变化不再处理。
- **第 133 行**：解释没有切换时按单方向整图处理。
- **第 134 行**：判断 `switch_index` 是否仍为 `None`。
- **第 135 行**：返回一个覆盖全高的区域及全局角度。
- **第 136 行**：空行。
- **第 137 行**：把条带索引换成实际 y 边界，`+0.5` 把边界放在条带中部。这是经验定位，不是像素级精确分界。
- **第 138 行**：返回上、下两个区域；上方沿全局角，下方沿正交角。

### 第 141–183 行：在单个方向区域里找每条种植行

- **第 141–145 行**：定义函数。输入局部掩膜、方向角、该局部区域在整张工作图中的 y 偏移；输出候选线段列表和估计行距。
- **第 146 行**：概括三步：投影找峰、拟合有限线段、映射回工作图。
- **第 147 行**：空行。
- **第 148 行**：取得把行旋到竖直方向的矩阵和新画布大小。
- **第 149 行**：执行仿射变换。二值掩膜使用最近邻插值，避免插值生成灰色中间值；画布外填 0（黑）。
- **第 150 行**：沿 y 方向统计每一列白色像素数量，得到横向一维曲线。旋正后，一条竖直作物行会使对应列附近出现高峰。
- **第 151 行**：平滑曲线。`sigma` 至少 1.5，并会随曲线长度增加，兼顾不同图像尺寸。[1]
- **第 152 行**：调用 V1 的自相关函数估计周期行距。它对曲线去均值，再寻找一维自相关在合理滞后区间内的峰。
- **第 153 行**：计算峰的最低显著度：取全局最大值的 6% 和标准差的 30% 中较大者。这样峰既要相对最高行够高，也要超过整体波动。
- **第 154 行**：调用 SciPy `find_peaks` 开始找局部峰。[2]
- **第 155 行**：输入是平滑后的横向投影曲线。
- **第 156 行**：相邻峰至少间隔 `0.55×行距`，且至少 8 像素；避免一条宽作物行被重复报成两条。
- **第 157 行**：要求峰满足第 153 行计算的显著度。
- **第 158 行**：结束调用。返回的第二项是峰属性字典，但此处用 `_` 表示不需要。
- **第 159 行**：空行。
- **第 160 行**：求旋转矩阵的逆矩阵，后面把旋转坐标还原到局部原坐标。
- **第 161 行**：创建空线段列表。每条元素包含起点、终点、支持点数、纵向覆盖率。
- **第 162 行**：每个峰左右的采样半宽为 `0.28×行距`，至少 4 像素。
- **第 163 行**：说明每峰只产一条中心线，且暂不解决畦内双行。
- **第 164 行**：逐个遍历峰的位置。
- **第 165 行**：调用 V1 的 `sample_row_points`：在峰附近走廊中沿 y 分箱，每个小箱用植被像素的 x/y 中位数产生一个代表点。这样大叶片不会仅因像素多就获得过高权重。
- **第 166 行**：说明短程支持不足的峰应被删除。
- **第 167 行**：两个淘汰条件用“或”连接：代表点少于 12，或这些点的 y 跨度不足旋转图高度的 18%。`np.ptp` 就是最大值减最小值。
- **第 168 行**：跳过当前峰，继续下一个。
- **第 169 行**：用 OpenCV `fitLine` 拟合直线。`DIST_HUBER` 是鲁棒损失，能减弱离群点影响；结果 `(vx,vy)` 是方向向量，`(x0,y0)` 是直线上一点。[5]
- **第 170 行**：说明旋正后拟合线理应接近竖直。
- **第 171 行**：若水平方向分量 `|vx|>0.25`，或 `vy` 几乎为零导致后面除法危险，则触发保守回退。
- **第 172 行**：强制方向为竖直 `(0,1)`，中心 x 使用样本横坐标中位数；这里保留原来的 `y0`。
- **第 173 行**：用 y 坐标的 2% 和 98% 分位数作线段上下端，排除极端离群点，同时让线段长度由真实植被支持决定。
- **第 174 行**：根据直线参数计算旋转坐标系中的起点，并补一个 1，形成齐次坐标 `[x,y,1]`，便于乘 2×3 仿射矩阵。
- **第 175 行**：同理计算终点。
- **第 176 行**：逆变换起点，回到未旋转的局部区域坐标。
- **第 177 行**：逆变换终点。
- **第 178 行**：说明局部图来自整图的一段纵向裁剪，必须补回 y 偏移。
- **第 179 行**：给起点 y 加 `y_offset`。
- **第 180 行**：给终点 y 加 `y_offset`。
- **第 181 行**：计算纵向覆盖率 = 支持点 y 跨度 / 旋转图高度，范围通常为 0–1；越大说明行的长程支持越充分。
- **第 182 行**：把该线段的四项信息加入列表。
- **第 183 行**：返回所有候选线段和工作尺度下的像素行距。

### 第 186–242 行：完整处理一张图片

- **第 186 行**：定义单图处理函数。输入图片路径、输出目录和参数；返回结果字典。
- **第 187 行**：说明会输出掩膜、叠加图和 JSON。
- **第 188 行**：空行。
- **第 189 行**：以彩色模式读取图片。结果为 BGR NumPy 数组。
- **第 190 行**：说明读图失败不能悄悄继续。
- **第 191 行**：判断 `original` 是否为 `None`。
- **第 192 行**：失败就抛出带路径的运行时异常。
- **第 193 行**：调用 V1 等比例缩小。若原图最长边不超过 `max_side`，只复制不放大；`scale` 是工作图/原图的比例。
- **第 194 行**：同时生成强掩膜和弱苗增强掩膜。
- **第 195 行**：计算增强掩膜中白色像素占整图的比例。
- **第 196 行**：说明比例异常常表示阈值失控。
- **第 197 行**：只接受 0.2%–65% 的植被占比。这是保护阈值，不是棉田通用定律。
- **第 198 行**：越界时抛异常，且保留四位小数方便诊断。
- **第 199 行**：空行。
- **第 200 行**：调用 V1：先对强掩膜做 Canny 边缘，再用概率 Hough 找线段，按线段长度加权聚类，得到全局轴向角和主簇线段数。概率 Hough 返回有限线段端点。[6]
- **第 201 行**：用强掩膜判断是一整块同方向，还是上下两个正交方向区域。
- **第 202 行**：创建总候选线段列表。
- **第 203 行**：创建区域记录列表，之后写入 JSON。
- **第 204 行**：说明分区的实际目的：避免上方纵行被错误延长到下方横向植被。
- **第 205 行**：遍历区域；`enumerate(...,start=1)` 同时给区域编号 1、2……，并拆出 y 范围和角度。
- **第 206 行**：裁出当前区域的增强掩膜，在里面检测行，并传入 `y0` 供坐标还原。
- **第 207 行**：把当前区域线段全部追加到整图线段列表。
- **第 208 行**：准备给区域记录列表追加一个字典。
- **第 209 行**：字典开始。
- **第 210 行**：保存区域编号。
- **第 211 行**：保存工作图坐标系中的 y 范围，采用 Python 切片语义 `[y0,y1)`，即含 y0、不含 y1。
- **第 212 行**：保存该区域轴向角。
- **第 213 行**：把工作尺度行距除以 `scale`，换算为原图像素行距。注意它仍是像素，不是厘米；要变成实地距离还需 GSD。
- **第 214 行**：保存最终长度过滤前的候选行数。
- **第 215–216 行**：结束字典和追加调用。
- **第 217 行**：空行。
- **第 218 行**：调用 V1 把工作坐标线段除以 `scale` 映射到原图、裁剪端点、过滤过短线、画橙色线和端点，并生成 `RowSegment` 对象列表。
- **第 219 行**：说明完全无可靠行应视为失败。
- **第 220 行**：判断最终行列表是否为空。
- **第 221 行**：为空就抛异常。
- **第 222 行**：把工作尺度增强掩膜放大回原图尺寸；仍用最近邻，保持只有 0/255。
- **第 223 行**：拼出掩膜输出路径。
- **第 224 行**：拼出叠加图输出路径。
- **第 225 行**：拼出单图 JSON 输出路径。
- **第 226 行**：写掩膜 PNG。当前代码没有检查 `cv2.imwrite` 的布尔返回值，因此磁盘写入失败可能不会在这里抛错。
- **第 227 行**：写叠加结果 PNG，同样未显式检查返回值。
- **第 228 行**：开始构造单图结果字典。
- **第 229 行**：记录算法版本为 V2。
- **第 230 行**：记录源图路径。
- **第 231 行**：记录原图宽高；NumPy 形状顺序是 `[高,宽,通道]`，所以宽取索引 1，高取索引 0。
- **第 232 行**：记录缩放比例。
- **第 233 行**：记录增强植被占比。
- **第 234 行**：记录全局 Hough 轴向角。
- **第 235 行**：记录支持主方向的 Hough 候选线数。
- **第 236 行**：记录方向区域数量。
- **第 237 行**：记录各区域详细信息。
- **第 238 行**：记录最终种植行数量。
- **第 239 行**：逐个把 `RowSegment` 数据类转成普通字典。字段含行号、起终点、角度、长度、支持点数、覆盖率。
- **第 240 行**：结束结果字典。
- **第 241 行**：把字典格式化为 UTF-8 JSON。`ensure_ascii=False` 让中文保持可读，`indent=2` 生成两空格缩进。
- **第 242 行**：返回结果，供批处理汇总使用。

### 第 245–271 行：程序入口与批处理

- **第 245 行**：定义主函数 `main`，组织整个批处理流程。
- **第 246 行**：说明 V2 使用独立输出目录和汇总，不主动覆盖 V1。
- **第 247 行**：空行。
- **第 248 行**：读取命令行参数。
- **第 249 行**：把输入目录字符串包装成 `Path`。
- **第 250 行**：把输出目录字符串包装成 `Path`。
- **第 251 行**：创建输出目录；`parents=True` 会连父目录一起创建，`exist_ok=True` 表示目录已存在也不报错。
- **第 252 行**：创建批处理摘要列表。
- **第 253 行**：说明只按明确文件名处理，不扫描整个目录。
- **第 254 行**：遍历 `args.names` 中的每个文件名。
- **第 255 行**：用输入目录和文件名拼出完整路径。
- **第 256 行**：开始 `try` 异常保护；单张图失败不会终止整个批次。
- **第 257 行**：处理当前图片。
- **第 258 行**：成功时追加源路径、成功状态和行数。
- **第 259 行**：在终端打印成功、行数和区域数。
- **第 260 行**：捕获当前图片处理中的任何普通异常，并命名为 `exc`。范围很宽，优点是批处理不中断，缺点是开发阶段可能掩盖具体异常类型。
- **第 261 行**：失败时记录源路径、失败状态和错误文本。
- **第 262 行**：在终端打印失败信息。
- **第 263 行**：准备写总汇文件 `run_summary_v2.json`。
- **第 264 行**：汇总内容包含本次实际参数和所有图片状态；`vars(args)` 把 Namespace 变成字典。
- **第 265 行**：指定 UTF-8。
- **第 266 行**：结束写文件调用。
- **第 267–268 行**：空行。
- **第 269 行**：解释下面的 Python 标准入口保护。
- **第 270 行**：只有直接执行此文件时，`__name__` 才等于 `"__main__"`；若它被别的脚本导入，则不会自动跑批处理。
- **第 271 行**：调用 `main()`，程序正式开始。

## 4. V2 实际复用了 V1 的哪些能力

| V2 调用位置 | V1 函数 | 实际作用 |
|---|---|---|
| 第 39 行 | `build_hsv_mask` | HSV 强植被分割、形态学清理、连通域面积过滤 |
| 第 152 行 | `estimate_spacing` | 用投影曲线自相关估计像素行距 |
| 第 165 行 | `sample_row_points` | 沿行走廊分箱，提取近似均匀的中心代表点 |
| 第 193 行 | `resize_for_work` | 等比例缩图并返回尺度比例 |
| 第 200 行 | `estimate_hough_angle` | Canny + 概率 Hough + 轴向角聚类，估计主方向 |
| 第 218 行 | `draw_and_serialize` | 坐标映回原图、长度过滤、画线、生成结构化行记录 |

## 5. 这段算法里最容易误解的 7 点

1. **Hough 不是直接给出最终种植行。** 它只负责估计整体方向；最终一条条行的位置来自旋正后的横向投影峰。
2. **`angle_deg` 是轴向角。** 一条线的 0° 和 180°代表同一轴，所以代码大量使用 `%180`。
3. **强掩膜和增强掩膜用途不同。** 强掩膜更可靠，用来定方向；增强掩膜保留更多弱苗，用来找每行的位置和跨度。
4. **“行距”只是图像像素行距。** 除非引入 GSD、航高与相机标定，否则不能直接解释成厘米。
5. **分区不是通用地块分割。** 它固定切 12 个水平条带，只支持一次从全局方向到正交方向的上下切换。
6. **多数阈值是经验参数。** 例如 ExG≥30、弱域≥6、正交得分×1.15、覆盖率 18%，不能当成跨地块普适的农学常数。
7. **输出线是有限线段。** 端点来自支持点 y 坐标的 2%/98% 分位数，不会无条件贯穿整幅图。

## 6. 独立审视：代码当前的边界与潜在风险

- 文件头和 `main` 的说明是“默认处理两张大图”，这是真的；但 `--names` 允许处理任意列出的图片。当前 `outputs_v2` 中存在 `photo1` 至 `photo12` 的结果，说明历史上曾以非默认参数运行。
- `detect_direction_regions` 只检测水平边界和一次 90° 转向；对斜边界、多地块、多方向但非正交、弯曲行并不适用。
- ExG 和 HSV 阈值受光照、白平衡、土壤颜色、相机响应和生育期影响。最好用独立标注集验证，而不是只在两张优化图上观察。
- 第 137 行的边界放在“首次两个正交条带中的第一个条带中心”，存在约一个条带尺度的定位误差。
- 第 226–227 行没有检查 `cv2.imwrite` 是否成功；更稳妥的工程实现应检查返回值。
- 第 260 行捕获所有 `Exception`，适合不中断批处理，但若做研发调试，建议同时保存 traceback。
- `row_count_before_final_filter` 不是最终区域行数，因为 V1 绘图阶段还会删除原图尺度下过短的线。

## 7. 术语小字典

- **掩膜（mask）**：与原图同宽高的黑白图；白色表示“选中”，黑色表示“未选中”。
- **连通域**：相邻白像素组成的一块区域，可近似看作一株苗或一个噪声斑。
- **形态学开运算**：先缩小白区再扩大，常用于去小白点。
- **膨胀**：扩大白色区域。
- **投影曲线**：按列或按某个方向统计白色像素后得到的一维数组。
- **峰值显著度（prominence）**：一个峰相对周围基线“突出多少”，不只是峰本身有多高。[2]
- **自相关**：把曲线和它平移后的自己比较；周期排列在接近周期长度的位置会重新相似。
- **仿射矩阵**：用一个 2×3 矩阵统一表示旋转和平移。
- **齐次坐标**：把 `[x,y]` 写成 `[x,y,1]`，从而可直接与 2×3 仿射矩阵相乘。
- **鲁棒拟合**：降低少量异常点对拟合直线的影响。

## 8. 参考资料

[1] SciPy Community. *scipy.ndimage.gaussian_filter1d — SciPy API Reference* [EB/OL]. SciPy Documentation, 2026. https://docs.scipy.org/doc/scipy/reference/generated/scipy.ndimage.gaussian_filter1d.html (accessed 2026-08-27).

[2] SciPy Community. *scipy.signal.find_peaks — SciPy API Reference* [EB/OL]. SciPy Documentation, 2026. https://docs.scipy.org/doc/scipy/reference/generated/scipy.signal.find_peaks.html (accessed 2026-08-27).

[3] OpenCV. *Morphological Transformations* [EB/OL]. OpenCV Documentation, 2026. https://docs.opencv.org/4.x/d9/d61/tutorial_py_morphological_ops.html (accessed 2026-08-27).

[4] OpenCV. *getRotationMatrix2D — Geometric Image Transformations* [EB/OL]. OpenCV Documentation, 2026. https://docs.opencv.org/4.x/da/d54/group__imgproc__transform.html (accessed 2026-08-27).

[5] OpenCV. *fitLine — Structural Analysis and Shape Descriptors* [EB/OL]. OpenCV Documentation, 2026. https://docs.opencv.org/4.x/d3/dc0/group__imgproc__shape.html (accessed 2026-08-27).

[6] OpenCV. *Hough Line Transform* [EB/OL]. OpenCV Documentation, 2026. https://docs.opencv.org/4.x/d9/db0/tutorial_hough_lines.html (accessed 2026-08-27).
